[project]
name = "lsflight-dev"
version = "0.1.0"
description = "Local build tools for YSFLIGHT development"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "chrome-agent>=0.6.0",
    "cmake>=3.31,<4",
    "ninja>=1.11,<2",
]

[tool.uv]
package = false
